[package]
name = "web_server"
version = "0.1.0"
edition = "2021"

[dependencies]

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rocket_cors = "0.6.0"

[dependencies.rocket]
version = "0.5.0-rc.1"
features = ["json"]